import assert = require('assert');
import {expect} from 'chai';

describe('util',
    function () {

        it('true is true',
            function () {
                assert.equal(true, true);
            });
    });
