import assert = require('assert');
import { expect } from 'chai';

describe('pak',
    function () {

        it('true is true',
            function () {
                assert.equal(true, true);
            });
    });
